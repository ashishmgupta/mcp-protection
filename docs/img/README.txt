Save screenshots from the conversation here with these exact filenames:

01-firewall-blocked.png     — Python client attack blocked (HTTP 403, RESULT: Blocked by Kong AI Firewall ✗)
02-claude-code-connected.png — claude mcp list showing jira-kong: Connected ✓
03-scrum1-result.png        — Claude Code session with SCRUM-1 table result + Kong logs

The HTML will automatically show the image if the file exists,
or fall back to a placeholder label if not.
